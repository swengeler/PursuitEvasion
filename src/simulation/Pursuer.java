package simulation;

public class Pursuer extends Agent {

    public Pursuer(double speed, double turnSpeed, double fieldOfViewAngle, double fieldOfViewRange) {
        super(speed, turnSpeed, fieldOfViewAngle, fieldOfViewRange);
    }

}
